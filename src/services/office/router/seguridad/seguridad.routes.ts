import { Router } from 'express';

const Seguridad: Router = Router();
//
//Seguridad.route('/departments').get(getDepartments);
//
//Seguridad.route('/permisos').get(getPermisos);
//
//Seguridad.route('/roles').get(getRoles);

export default Seguridad;
